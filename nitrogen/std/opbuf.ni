fn native start()
fn native stop()
fn native isStarted()
fn native clear()
fn native flush()
fn native get()
fn native stopAndGet()

return {
    "start": start,
    "stop": stop,
    "isStarted": isStarted,
    "clear": clear,
    "flush": flush,
    "get": get,
    "stopAndGet": stopAndGet,
}
