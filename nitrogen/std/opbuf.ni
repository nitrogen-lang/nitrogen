fn native start()
fn native stop()
fn native isStarted()
fn native clear()
fn native flush()
fn native get()
fn native stopAndGet()

return {
    start,
    stop,
    isStarted,
    clear,
    flush,
    get,
    stopAndGet,
}
