fn native cwd()
fn native dir(path)
fn native basename(path)
fn native ext(path)
fn native abs(path)
fn native join(paths)

return {
    "cwd": cwd,
    "dir": dir,
    "basename": basename,
    "ext": ext,
    "abs": abs,
    "join": join,
}
