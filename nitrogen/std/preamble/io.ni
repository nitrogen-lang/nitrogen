fn native print()
fn native printlnb()
fn native println()
fn native printerr()
fn native printerrln()
fn native printenv()
fn native varDump()
fn native readline()

return {
    print,
    printlnb,
    println,
    printerr,
    printerrln,
    printenv,
    varDump,
    readline,
}
